version: '3.9'

services:
  frontend:
    container_name: sun-fe
    image: sun-fe  
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/app
