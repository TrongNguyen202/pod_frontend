version: '3.9'

services:
  frontend:
    container_name: frontend-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env  # Load tất cả biến từ file .env
    restart: unless-stopped
    volumes:
      - .:/app
